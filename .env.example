###############################################
# Python Worker Environment Example
# Copy to .env and fill in real values (do NOT commit secrets).
###############################################

# (Optional) FastAPI listen port (defaults to 8000 in Dockerfile CMD)
# PORT=8000

# Redis URL (not currently used directly here, but keep for future queue integration)
# REDIS_URL=redis://redis:6379

# O*NET enrichment configuration
# Comma-separated SOC codes used to enrich job requirements.
ONET_SKILL_CODES=15-1252.00,15-1241.00,15-1221.00
# Minimum relevance score (0-100) when filtering O*NET derived skills.
ONET_MIN_RELEVANCE=80
# O*NET Basic auth credentials (treat as secrets)
ONET_USER=
ONET_PASSWORD=

# Gemini / LLM Extraction (optional)
GEMINI_API_KEY=
# Model name (e.g., gemini-1.5-flash, gemini-2.5-flash)
GEMINI_MODEL=gemini-1.5-flash
# Thinking budget tokens (if the model supports structured reasoning; else leave 0)
GEMINI_THINKING_BUDGET=0
# Whether to include reasoning tokens in responses (may expand payload size)
GEMINI_INCLUDE_THOUGHTS=false

# Toggle use of inferred requirements in match scoring (overridden in backend if desired)
USE_INFERRED_REQUIREMENTS=false

# Logging level (INFO, WARNING, DEBUG)
# LOG_LEVEL=INFO

###############################################
# Notes:
# - Add only the variables you need; all others safely default.
# - Keep .env out of version control (.gitignore).
# - Backend compose overrides PYTHON_SERVICE_URL internally; no need to set it here.
###############################################
# Python worker environment variables
# O*NET credentials (optional for enrichment). If absent, enrichment is skipped.
ONET_USER=
ONET_PASSWORD=
# (Deprecated) ONET_SKILL_CODES retained only for backward compatibility; ignored by current code path.
ONET_SKILL_CODES=15-1252.00,15-1211.00,13-1111.00

# Gemini (Google Generative AI) optional technology extraction
# Set GEMINI_API_KEY to enable model-based tech skill extraction in job parsing.
GEMINI_API_KEY=
# Optional model override (defaults to gemini-1.5-flash if blank)
GEMINI_MODEL=
# Optional reasoning (thinking) token budget for 2.5 models. 0 disables, -1 dynamic, >0 explicit budget.
GEMINI_THINKING_BUDGET=
# Include thought summaries (true/false). Usually not needed for simple extraction.
GEMINI_INCLUDE_THOUGHTS=
